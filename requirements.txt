# Python package requirements for the Financial Inclusion ML mini-project.
#
# Install everything with:
#     pip install -r requirements.txt
#
# The >= pins name the minimum version each script was written against.
# Any later version in the same major line should also work.
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
numpy>=1.24